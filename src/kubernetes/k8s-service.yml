apiVersion: v1
kind: Service
metadata:
  name: npd-app-service
  namespace: npd-space
  labels:
    app: npd-app
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: npd-app